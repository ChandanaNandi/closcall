# Copy to .env (gitignored). Secrets NEVER appear in this file (Bible §6).
# Environment variables use the CLOSCALL_ prefix; nested settings use __ (Bible §6).
CLOSCALL_ENVIRONMENT=dev
CLOSCALL_LOGGING__LEVEL=INFO
