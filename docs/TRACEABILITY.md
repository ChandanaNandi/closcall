# ClosCall — Traceability Map (acceptance row J07)

*Every public claim traces to an artifact.* This is the master evidence index: for each acceptance
section it names the concrete evidence (artifact file, `make` target, test module, status note, or
manifest hash) that backs the row. Rows waived under Amendment A1 (ADR-001) are marked **[WAIVED]**
and point to the backlog. Regenerate the result numbers with `make reports-v3` / `make readme-tables`.

## Release anchors (the immutable run ids)
| anchor | manifest | run id | scope |
|---|---|---|---|
| **v3 (release headline)** | `artifacts/manifests/gate12_5-dataset-v3.json` (`dd8def51705710fa…`) | `gate8-full-corpus-v3` (under load) | detection + localization ablation |
| v2 (retained, immutable) | `artifacts/manifests/gate9-dataset.json` (`58d2cf31a55348bd…`) | `gate8-full-corpus-v2` (traffic-free) | superseded baseline; preserved |

Every README result number is generated by `make readme-tables` from the v3 manifest and its
content-bound artifacts — none are hand-typed (verified: no result numbers appear in README outside
the `BEGIN/END GENERATED` block).

## A. Planning and reproducibility
| row | evidence |
|---|---|
| A01 no unresolved P0 contradiction | `docs/decisions/ADR-001` reconciled matrix vs README; canon precedence in `README.md` |
| A02 exact version/digest per dep/image/model | `docs/toolchain.md`; `uv.lock`; image digests in both manifests; LLM digests `7cdf5a0187d5`/`845dbda0ea48` |
| A03 source claims pinned to commit + access date | `research/source-register.md`; NIKA pinned `sands-lab/nika @ e6649f45…` (`gate12-nika.txt`) |
| A04 clean clone → identical generated configs | `make render` + `make render-validate` / `make fabric-validate`; config_hash in manifest |
| A05 every result identifies code/lock/image/model/dataset/prompt/config/seeds | `gate12_5-dataset-v3.json` (code_revision, dependency_lock_hash, image_digests, split, seeds, content_hashes) |

## B. Network fabric (Gate 2/3)
| row | evidence |
|---|---|
| B01–B08 fabric/IPAM/BGP/RIB/FIB/reachability/MTU | `make fabric-validate`, `make lab-check` (B03–B08); `scripts/gate3_accept.py`; `gate3-run1.txt`, `gate3-run2.txt` |
| B09 ECMP distribution | `scripts/b09_ecmp.py`; `gate6-slice.txt` |
| B10–B11 path/spine failure + restoration timelines | `scripts/b10_b11_convergence.py`, `scripts/measure_convergence.py` |
| B12 teardown removes all residue | `scripts/b12_teardown.py`; `make lab-down` |

## C. Telemetry integrity (Gate 4)
| row | evidence |
|---|---|
| C01–C08 YANG paths, cardinality, timestamps, clock, counter golden tests, stale-data signal, replay | `make telemetry-check`; `scripts/telemetry_check.py`; `gate4-telemetry.txt` |

## D. Fault framework (Gate 5)
| row | evidence |
|---|---|
| D01–D08 write-ahead, onset labels, impairment verify, controls/hard-negatives, no-cleanup-as-remediation, reconciliation, quarantine | `make fault-smoke`; `scripts/fault_smoke.py`; `artifacts/chaos-ledger.jsonl`; corpus quarantine of 2 v3 incidents (manifest `exclusions`) |

## E. Incidents and data (Gate 7)
| row | evidence |
|---|---|
| E01 migrations vs PostgreSQL | `make db-migrate`; `migrations/`, `alembic.ini` |
| E02/E07/I07 runtime role cannot read ground truth/DDL | `scripts/db_isolation_check.py`; `make db-isolation`; `gate7-db.txt` |
| E03/E04 dedup + versioned incident transitions | `core.incidents`/`incident_events` state tests (Gate 7); `gate7-corpus.txt` |
| E05 content-addressed artifacts | `Artifact.sha256`; manifest `content_hashes`; `scripts/corpus_verify.py` (`make corpus-verify`) |
| E06 split proves no incident/link/leaf/seed/time overlap | `src/closcall/datasets/splits.py` (location-inductive); `split_manifest_hash` in manifest; `docs/DATA_CARD.md` §splits |
| E08 online/offline causal parity | `src/closcall/datasets/features.py` (strictly-causal, `as_of_at` cutoff) |

## F. Sensors and evaluation (Gate 9 / 12.5)
| row | evidence |
|---|---|
| F01 identical causal inputs + event matching | `scripts/evaluate_sensors_v3.py` + `src/closcall/sensors/`; common-window guard (§10.3) |
| F02 thresholds tuned on validation, never test | `freeze_on_train_val` (grid search on train+val only); `gate12_5-detection-v3.txt` |
| F03 detection reports misses/FP/coverage/latency/CIs | `gate12_5-detection-v3.txt` (recall 0.60, per-class, latency, 95% CI) |
| F04/F05 link-ranking MLP + no-message baseline; GNN | `scripts/gate12_5_localization_ablation.py` (RULE vs MLP §11.7 vs GNN §11.8) |
| F06 IDs excluded + permutation/topology invariance | ablation features exclude IDs; `healthy_control` at chance (no position leakage) — `localization-v3.txt` |
| F07 topology-transfer / location-inductive kept separate | location-inductive split reported distinctly; `docs/MODEL_CARD.md` |
| F08 strata, seeds, incident-clustered 95% CIs | `gate12_5-localization-v1.txt`/`-v2.txt` (per-class CIs); `gate12_5-localization-cv.txt` (leave-one-leaf-out) |

## G. Evidence and reasoning (Gate 10)
| row | evidence |
|---|---|
| G01–G08 typed claims, adversarial fixtures, no fabricated certainty, tool envelope, injection resistance, deterministic fallback, policy-only ablation | `evals/reports/gate10-status.md`; modules `evidence/claims.py`, `evidence/tools.py`, `workflow/diagnose.py`, `workflow/report.py`, `workflow/llm.py`; `make qualify-llm` → `gate10-llm.txt`; R30 relevance-gate fix |

## H. Approval and execution (Gate 11)
| row | evidence |
|---|---|
| H01 immutable SHA-256 plan | `executor/plan.py`; `gate11-status.md` |
| H02 decision binds version/digest/topology **[WAIVED: expiry]** | `executor/prechecks.py`; backlog item 1 |
| H03 edit/drift blocks execution **[WAIVED: expiry/revocation blocking]** | `test_prechecks` (edited digest + topology drift fail closed) |
| H04/H05 API/workflow no device creds; executor no public ingress | trust-boundary tests; `gate11-status.md` |
| H06 duplicate execution prevented **[WAIVED: leasing/outbox]** | `ExecutionJob` unique + idempotency key |
| H07 last-path/mgmt-iface/stale-telemetry/headroom fail closed **[A2 PARTIAL]** | full suite `run_prechecks` unit-proven (`test_prechecks`); LIVE `execute_job` enforces subset (approval-digest + allowlist + mgmt-iface); full wiring waived — ADR-004, backlog item 7 |
| H08 pre-state + rollback captured before mutation | executor `before` capture + pre-mutation audit |
| H09 timeout → outcome_unknown + reconcile | `executor.reconcile_job`; `gate11-status.md` |
| H10/H11 recovery predicates; rollback reverses or exposes failure | `executor/rollback.py`; `test_rollback_audit` |

## I. Security and audit (Gate 1/11)
| row | evidence |
|---|---|
| I01 TLS + separate read/write identities | lab PKI `make pki`, `scripts/gen_pki.py` |
| I02 JWT/RBAC/IDOR/secure-cookie/CSRF **[WAIVED: rotation/rate-limit]** | `test_auth`, `test_api`; `api/auth.py`, `api/app.py` |
| I02a UI loopback-HTTPS only; no Secure cookie over HTTP | session cookie `Secure=True`; `docs/threat-model.md` |
| I03 no secret in source/logs/artifacts | `make secret-scan` (gitleaks); `.env`/PKI gitignored |
| I04 least-privilege containers + pinned digests | `make sbom`, `make dep-audit`; `compose.yaml`; image digests |
| I05 audit append-only + FK **[WAIVED: hash chain]** | `audit.events` FK'd, never updated/deleted |
| I06 audit failure blocks mutation | executor audit flush-guarded before device mutation |
| I07 runtime identity cannot read ground truth | `make db-isolation`; `gate7-db.txt` |

## J. Operations and final proof (Gate 12 / 13)
| row | evidence |
|---|---|
| J01 measured resource headroom | Gate 12 resource report; `gate12-status.md` |
| J02 sharding from measured capacity | scheduler report; `gate12-status.md` |
| J03 executor + PostgreSQL restart determinism **[WAIVED: full matrix]** | failure suite; `gate11-status.md` |
| J04 backup/restore **[WAIVED — backlog]** | `docs/backlog.md` item 6 |
| J05 FRR CI + SR Linux acceptance | CI reports; `scripts/validate_srl_configs.py` |
| **J06 clean-clone demo deploy→teardown** | `make demo` (Gate 13 — see `runbooks/operator-guide.md`) |
| **J07 every README number from immutable run id** | `make readme-tables` (this section + `docs/RESULTS.md`); anchor `dd8def51…` |
| **J08 fidelity limits + negative findings published** | `docs/LIMITATIONS.md` |

## Master claim
No "complete/autonomous/safe/validated" claim is made beyond the green rows above; every waived row is
marked here and in `planning/05-Acceptance-Matrix.md`, justified by `docs/decisions/ADR-001`, and
listed as a limitation in `docs/LIMITATIONS.md` (satisfying the A1 Master release rule).
